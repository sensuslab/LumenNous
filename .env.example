# The reviewed local Create Engine remains the default and needs no key.
# The optional server-only DeepSeek route is disabled until explicitly enabled.
DEEPSEEK_AI_ENABLED=false
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
